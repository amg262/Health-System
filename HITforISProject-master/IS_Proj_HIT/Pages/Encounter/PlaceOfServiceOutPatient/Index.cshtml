@page
@model IS_Proj_HIT.IndexModelPlaceOfServiceOutPatient

@{
    ViewData["Title"] = "Place of Service Out Patient";
}

<div class="row my-3">
<h3>
    <a class="btn btn-primary" asp-controller="Administration" asp-action="EncounterDataEntry">
        <i class="fa fa-arrow-left "></i>
        </a>
      Place of Service Out Patient
</h3>
</div>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PlaceOfServiceOutPatient[0].WiPopCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlaceOfServiceOutPatient[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlaceOfServiceOutPatient[0].LastModified)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.PlaceOfServiceOutPatient)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.WiPopCode)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastModified)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.PlaceOfServiceId">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.PlaceOfServiceId">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.PlaceOfServiceId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
