@page
@model IS_Proj_HIT.IndexModelBloodPressureRouteType

@{
    ViewData["Title"] = "Blood Pressure Route Type";
}
<div class="row my-3">
<h3>
     <a class="btn btn-primary" asp-controller="Administration" asp-action="DataEntry">
            <i class="fa fa-arrow-left "></i>
        </a>
    Blood Pressure Route Type
</h3>
</div>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BloodPressureRouteType[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BloodPressureRouteType[0].LastModified)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.BloodPressureRouteType)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastModified)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.BloodPressureRouteTypeId">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.BloodPressureRouteTypeId">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.BloodPressureRouteTypeId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
