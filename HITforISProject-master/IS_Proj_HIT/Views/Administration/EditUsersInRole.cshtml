@model List<IS_Proj_HIT.ViewModels.UserRoleViewModel>

@{
    var roleId = ViewBag.roleId;
}
 <form method="post">
    <div class="card">
        <div class="card-header">
            <h2>
                Add or remove users from this role
                <input type="submit" value="Update" class="btn btn-primary float-right mx-4 mt-3" style="width: auto" />
                <a asp-action="EditRole" asp-route-id="@roleId" class="btn btn-outline-primary float-right mt-3" style="width: auto">Return to Edit Role</a>
            </h2>
           
        </div>
        @*<div class="card-body">
            @for (int i = 0; i < Model.Count; i++)
            {
            <div class="form-check m-1">
                <input type="hidden" asp-for="@Model[i].UserId" />
                <p asp-for="@Model[i].UserName" />
                <input asp-for="@Model[i].IsSelected" class="form-check-input" />
                <label class="form-check-label" asp-for="@Model[i].IsSelected">
                    @Model[i].FullName
                </label>
                <input asp-for="@Model[i].UserName" />
            </div>

            }
        </div>*@
        <table class="table table-striped table-sm ">
            <thead class="bg-primary text-white">
                <tr>
                    <th>Name</th>
                    <th>User Name</th>
                    <th>Add/Remove</th>
                </tr>
            </thead>
            <tbody>
                @for(int i = 0; i < Model.Count; i++)
                {
                <tr id="userRoleTableRow">

                    <td style="display:none"><input asp-for="@Model[i].UserId"></td>
                    <td style="display:none"> <input asp-for="@Model[i].UserName"></td>
                    <td style="display:none"> <input asp-for="@Model[i].FullName"></td>

                    <td>@Model[i].FullName</td>
                    <td>@Model[i].UserName</td>
                    <td>
                        <input asp-for="@Model[i].IsSelected" class="form-check-input ml-4 justify-center" style="width: 30px; height: 25px; margin-left: 38px;" />
                    </td>
                </tr>
                }
            </tbody>
        </table>
        <div class="card-footer">
            <input type="submit" value="Update" class="btn btn-primary float-right mx-4" style="width: auto" />
            <a asp-action="EditRole" asp-route-id="@roleId" class="btn btn-outline-primary float-right" style="width: auto">Return to Edit Role</a>
        </div>
    </div>
 </form>
